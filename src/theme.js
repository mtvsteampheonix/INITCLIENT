import {createTheme} from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#4199e1'
    },
    gray: {
      main: '#A9A9A9',
      contrastText: '#fff'
    },
    initRed: {
      main: '#FF0000',
      contrastText: '#fff'
    }
  }
});
